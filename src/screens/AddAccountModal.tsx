import { View, Text, StyleSheet } from "react-native";
import React from "react";

export const AddAccountModal = () => {
  return (
    <View>
      <Text style={styles.text}>AddAccountModal</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  text: {
    color: "white",
  },
});
